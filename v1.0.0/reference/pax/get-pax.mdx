---
title: "Get PAX"
api: "GET /pax"
description: "Retrieve list of PAX (Platform Access eXchange) configurations and status"
---

Retrieve a comprehensive list of PAX configurations, which manage platform access and data exchange between different systems and organizations in the Weir AI ecosystem.

<Note>
  **Console API Endpoint**
  
  This endpoint is part of the Console API category and requires Bearer Token authentication. It's designed for platform access management and data exchange.
</Note>

## Authentication

This endpoint requires Bearer Token authentication. Include your JWT token in the Authorization header:

```bash
Authorization: Bearer <your_jwt_token>
```

## Query Parameters

<ParamField query="page" type="string" required="false">
  Page number for pagination (default: 1)
  
  **Example**: "1"
</ParamField>

<ParamField query="limit" type="string" required="false">
  Number of PAX configurations per page (default: 20, max: 100)
  
  **Example**: "20"
</ParamField>

<ParamField query="status" type="string" required="false">
  Filter PAX configurations by status
  
  **Options**: "active", "inactive", "pending", "error"
  
  **Example**: "active"
</ParamField>

<ParamField query="type" type="string" required="false">
  Filter PAX configurations by type
  
  **Options**: "data_exchange", "api_access", "webhook", "sso"
  
  **Example**: "data_exchange"
</ParamField>

<ParamField query="organization" type="string" required="false">
  Filter PAX configurations by organization
  
  **Example**: "org_1234567890"
</ParamField>

<ParamField query="search" type="string" required="false">
  Search PAX configurations by name or description
  
  **Example**: "customer data sync"
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="array">
  Array of PAX configuration objects
</ResponseField>

<ResponseField name="data[].pax_id" type="string">
  Unique identifier for the PAX configuration
</ResponseField>

<ResponseField name="data[].name" type="string">
  Name of the PAX configuration
</ResponseField>

<ResponseField name="data[].description" type="string">
  Description of the PAX configuration
</ResponseField>

<ResponseField name="data[].type" type="string">
  Type of PAX configuration
</ResponseField>

<ResponseField name="data[].status" type="string">
  Current status of the PAX configuration
</ResponseField>

<ResponseField name="data[].source_organization" type="string">
  Source organization ID
</ResponseField>

<ResponseField name="data[].target_organization" type="string">
  Target organization ID
</ResponseField>

<ResponseField name="data[].permissions" type="array">
  List of permissions granted
</ResponseField>

<ResponseField name="data[].data_schema" type="object">
  Data schema configuration
</ResponseField>

<ResponseField name="data[].last_sync" type="string">
  ISO 8601 timestamp of last data synchronization
</ResponseField>

<ResponseField name="data[].sync_frequency" type="string">
  Data synchronization frequency
</ResponseField>

<ResponseField name="data[].created_at" type="string">
  ISO 8601 timestamp when the PAX was created
</ResponseField>

<ResponseField name="pagination" type="object">
  Pagination information
</ResponseField>

<ResponseField name="message" type="string">
  Human-readable message about the operation result
</ResponseField>

<ResponseField name="timestamp" type="string">
  ISO 8601 timestamp of the response
</ResponseField>

## Example Request

<CodeGroup>
```bash cURL
curl -X GET "https://api.example.com/pax?status=active&type=data_exchange" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch('https://api.example.com/pax?status=active&type=data_exchange', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

params = {
    'status': 'active',
    'type': 'data_exchange'
}

response = requests.get(
    'https://api.example.com/pax',
    headers={
        'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
        'Content-Type': 'application/json'
    },
    params=params
)

print(response.json())
```
</CodeGroup>

## Example Response

```json
{
  "success": true,
  "data": [
    {
      "pax_id": "pax_1234567890abcdef",
      "name": "Customer Data Sync",
      "description": "Synchronize customer data between CRM and analytics platform",
      "type": "data_exchange",
      "status": "active",
      "source_organization": "org_1234567890",
      "target_organization": "org_0987654321",
      "permissions": [
        "read:customer_data",
        "write:analytics_data"
      ],
      "data_schema": {
        "customer_id": "string",
        "name": "string",
        "email": "string",
        "created_at": "datetime"
      },
      "last_sync": "2024-01-15T10:00:00Z",
      "sync_frequency": "hourly",
      "created_at": "2024-01-01T00:00:00Z"
    },
    {
      "pax_id": "pax_0987654321fedcba",
      "name": "API Access Gateway",
      "description": "Provide API access to external partners",
      "type": "api_access",
      "status": "active",
      "source_organization": "org_1234567890",
      "target_organization": "org_5555555555",
      "permissions": [
        "read:public_data",
        "read:partner_data"
      ],
      "data_schema": {
        "endpoint": "string",
        "method": "string",
        "rate_limit": "integer"
      },
      "last_sync": "2024-01-15T10:30:00Z",
      "sync_frequency": "real_time",
      "created_at": "2024-01-05T00:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 2,
    "total_pages": 1,
    "has_next": false,
    "has_prev": false
  },
  "message": "PAX configurations retrieved successfully",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

## Error Responses

<ResponseField name="401 Unauthorized" type="object">
  Invalid or expired authentication token
</ResponseField>

<ResponseField name="403 Forbidden" type="object">
  Insufficient permissions to view PAX configurations
</ResponseField>

<ResponseField name="400 Bad Request" type="object">
  Invalid query parameters
</ResponseField>

<ResponseField name="429 Too Many Requests" type="object">
  Rate limit exceeded
</ResponseField>

## Usage Notes

<AccordionGroup>
  <Accordion title="PAX Types" icon="link">
    Available PAX types:
    
    - **data_exchange**: Bidirectional data synchronization
    - **api_access**: API access and integration
    - **webhook**: Event-driven notifications
    - **sso**: Single Sign-On integration
  </Accordion>
  
  <Accordion title="Permissions" icon="shield">
    Common PAX permissions:
    
    - **read**: Read access to data
    - **write**: Write access to data
    - **execute**: Execute operations
    - **admin**: Administrative access
  </Accordion>
  
  <Accordion title="Sync Frequencies" icon="clock">
    Available sync frequencies:
    
    - **real_time**: Immediate synchronization
    - **hourly**: Sync every hour
    - **daily**: Sync once per day
    - **weekly**: Sync once per week
    - **manual**: Manual synchronization only
  </Accordion>
</AccordionGroup>

## Related Endpoints

<CardGroup cols={2}>
  <Card title="Create PAX" icon="plus" href="/v1.0.0/reference/console/pax/create-pax">
    `POST /pax` - Create a new PAX configuration
  </Card>
  <Card title="Update PAX" icon="edit">
    `PUT /pax/{paxId}` - Update PAX configuration
  </Card>
  <Card title="Delete PAX" icon="trash">
    `DELETE /pax/{paxId}` - Remove PAX configuration
  </Card>
  <Card title="Sync PAX" icon="refresh">
    `POST /pax/{paxId}/sync` - Trigger manual synchronization
  </Card>
</CardGroup>
