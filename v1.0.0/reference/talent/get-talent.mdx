---
title: "Get Talent"
api: "GET /talent"
description: "Retrieve list of talent profiles and their information"
---

Retrieve a comprehensive list of talent profiles in the Weir AI platform, including their skills, availability, and performance metrics. This endpoint is designed for talent management and recruitment purposes.

<Note>
  **Console API Endpoint**
  
  This endpoint is part of the Console API category and requires Bearer Token authentication. It's designed for talent management and recruitment.
</Note>

## Authentication

This endpoint requires Bearer Token authentication. Include your JWT token in the Authorization header:

```bash
Authorization: Bearer <your_jwt_token>
```

## Query Parameters

<ParamField query="page" type="string" required="false">
  Page number for pagination (default: 1)
  
  **Example**: "1"
</ParamField>

<ParamField query="limit" type="string" required="false">
  Number of talent profiles per page (default: 20, max: 100)
  
  **Example**: "20"
</ParamField>

<ParamField query="skills" type="string" required="false">
  Filter by specific skills (comma-separated)
  
  **Example**: "javascript,python,react"
</ParamField>

<ParamField query="availability" type="string" required="false">
  Filter by availability status
  
  **Options**: "available", "busy", "unavailable"
  
  **Example**: "available"
</ParamField>

<ParamField query="experience_level" type="string" required="false">
  Filter by experience level
  
  **Options**: "junior", "mid", "senior", "expert"
  
  **Example**: "senior"
</ParamField>

<ParamField query="location" type="string" required="false">
  Filter by location or timezone
  
  **Example**: "US", "Europe", "Asia"
</ParamField>

<ParamField query="search" type="string" required="false">
  Search talent by name or description
  
  **Example**: "machine learning engineer"
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="array">
  Array of talent profile objects
</ResponseField>

<ResponseField name="data[].talent_id" type="string">
  Unique identifier for the talent profile
</ResponseField>

<ResponseField name="data[].name" type="string">
  Name of the talent
</ResponseField>

<ResponseField name="data[].title" type="string">
  Professional title or role
</ResponseField>

<ResponseField name="data[].skills" type="array">
  List of skills and technologies
</ResponseField>

<ResponseField name="data[].experience_level" type="string">
  Experience level (junior, mid, senior, expert)
</ResponseField>

<ResponseField name="data[].availability" type="string">
  Current availability status
</ResponseField>

<ResponseField name="data[].location" type="string">
  Location or timezone
</ResponseField>

<ResponseField name="data[].rating" type="number">
  Overall performance rating (1-5)
</ResponseField>

<ResponseField name="data[].projects_completed" type="integer">
  Number of completed projects
</ResponseField>

<ResponseField name="data[].hourly_rate" type="number">
  Hourly rate in USD
</ResponseField>

<ResponseField name="data[].bio" type="string">
  Professional biography
</ResponseField>

<ResponseField name="data[].portfolio_url" type="string">
  URL to portfolio or work samples
</ResponseField>

<ResponseField name="pagination" type="object">
  Pagination information
</ResponseField>

<ResponseField name="message" type="string">
  Human-readable message about the operation result
</ResponseField>

<ResponseField name="timestamp" type="string">
  ISO 8601 timestamp of the response
</ResponseField>

## Example Request

<CodeGroup>
```bash cURL
curl -X GET "https://api.example.com/talent?skills=javascript,python&experience_level=senior" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch('https://api.example.com/talent?skills=javascript,python&experience_level=senior', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

params = {
    'skills': 'javascript,python',
    'experience_level': 'senior'
}

response = requests.get(
    'https://api.example.com/talent',
    headers={
        'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
        'Content-Type': 'application/json'
    },
    params=params
)

print(response.json())
```
</CodeGroup>

## Example Response

```json
{
  "success": true,
  "data": [
    {
      "talent_id": "talent_1234567890abcdef",
      "name": "Sarah Johnson",
      "title": "Senior Full Stack Developer",
      "skills": ["javascript", "python", "react", "node.js", "postgresql"],
      "experience_level": "senior",
      "availability": "available",
      "location": "US",
      "rating": 4.8,
      "projects_completed": 45,
      "hourly_rate": 85,
      "bio": "Experienced full-stack developer with 8+ years in web development",
      "portfolio_url": "https://portfolio.example.com/sarah-johnson"
    },
    {
      "talent_id": "talent_0987654321fedcba",
      "name": "Michael Chen",
      "title": "Machine Learning Engineer",
      "skills": ["python", "tensorflow", "pytorch", "scikit-learn", "aws"],
      "experience_level": "senior",
      "availability": "busy",
      "location": "Asia",
      "rating": 4.9,
      "projects_completed": 32,
      "hourly_rate": 95,
      "bio": "ML engineer specializing in deep learning and computer vision",
      "portfolio_url": "https://portfolio.example.com/michael-chen"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 2,
    "total_pages": 1,
    "has_next": false,
    "has_prev": false
  },
  "message": "Talent profiles retrieved successfully",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

## Error Responses

<ResponseField name="401 Unauthorized" type="object">
  Invalid or expired authentication token
</ResponseField>

<ResponseField name="403 Forbidden" type="object">
  Insufficient permissions to view talent profiles
</ResponseField>

<ResponseField name="400 Bad Request" type="object">
  Invalid query parameters
</ResponseField>

<ResponseField name="429 Too Many Requests" type="object">
  Rate limit exceeded
</ResponseField>

## Usage Notes

<AccordionGroup>
  <Accordion title="Talent Categories" icon="users">
    Available talent categories:
    
    - **Developers**: Software developers and engineers
    - **Designers**: UI/UX and graphic designers
    - **Data Scientists**: Data analysts and ML engineers
    - **DevOps**: Infrastructure and deployment specialists
    - **Consultants**: Business and technical consultants
  </Accordion>
  
  <Accordion title="Skills Matching" icon="target">
    Skills matching features:
    
    - Exact skill matching
    - Related skills suggestions
    - Skill level assessment
    - Technology stack compatibility
  </Accordion>
  
  <Accordion title="Availability Status" icon="clock">
    Availability statuses:
    
    - **available**: Ready for new projects
    - **busy**: Currently working on projects
    - **unavailable**: Not accepting new work
  </Accordion>
</AccordionGroup>

## Related Endpoints

<CardGroup cols={2}>
  <Card title="Get Talent Profile" icon="user" href="/v1.0.0/reference/console/talent/get-talent-profile">
    `GET /talent/{talentId}` - Get detailed talent profile
  </Card>
  <Card title="Hire Talent" icon="handshake">
    `POST /talent/{talentId}/hire` - Hire a talent for a project
  </Card>
  <Card title="Rate Talent" icon="star">
    `POST /talent/{talentId}/rate` - Rate talent performance
  </Card>
  <Card title="Get Talent Reviews" icon="message-square">
    `GET /talent/{talentId}/reviews` - View talent reviews and feedback
  </Card>
</CardGroup>
