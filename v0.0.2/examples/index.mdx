---
title: "Code Examples"
description: "Real-world code examples and integration patterns for Weir API"
---

import { Card, CardGroup } from '@mintlify/components';
import { Callout } from '@mintlify/components';

# Code Examples

Real-world code examples and integration patterns to help you implement Weir's features effectively in your applications.

## Available Examples

<CardGroup cols={2}>
  <Card
    title="JavaScript Examples"
    icon="javascript"
    href="/v0.0.2/examples/javascript"
  >
    Complete JavaScript examples for browser and Node.js environments
  </Card>
  <Card
    title="Python Examples"
    icon="python"
    href="/v0.0.2/examples/python"
  >
    Python SDK examples and integration patterns
  </Card>
  <Card
    title="React Examples"
    icon="react"
    href="/v0.0.2/examples/react"
  >
    React components and hooks for Weir integration
  </Card>
  <Card
    title="Vue.js Examples"
    icon="vue"
    href="/v0.0.2/examples/vue"
  >
    Vue.js components and composables for Weir integration
  </Card>
  <Card
    title="Node.js Examples"
    icon="nodejs"
    href="/v0.0.2/examples/nodejs"
  >
    Node.js backend service examples and patterns
  </Card>
  <Card
    title="PHP Examples"
    icon="php"
    href="/v0.0.2/examples/php"
  >
    PHP integration examples and Laravel packages
  </Card>
  <Card
    title="cURL Examples"
    icon="terminal"
    href="/v0.0.2/examples/curl"
  >
    Command-line examples using cURL for testing and automation
  </Card>
  <Card
    title="Postman Collection"
    icon="postman"
    href="/v0.0.2/examples/postman"
  >
    Import our Postman collection for easy API testing
  </Card>
</CardGroup>

## Quick Start Examples

### 1. Basic Authentication (JavaScript)
```javascript
// Basic authentication example
async function authenticateWithWeir(clientId, secretKey) {
  try {
    const response = await fetch('https://api-dev.weir.ai/auth/token', {
      method: 'POST',
      headers: {
        'Authorization': 'Basic ' + btoa(`${clientId}:${secretKey}`)
      }
    });
    
    if (!response.ok) {
      throw new Error(`Authentication failed: ${response.status}`);
    }
    
    const data = await response.json();
    return data.data.accessToken;
  } catch (error) {
    console.error('Authentication error:', error);
    throw error;
  }
}
```

### 2. Making API Requests (Python)
```python
import requests
from requests.auth import HTTPBasicAuth

class WeirAPIClient:
    def __init__(self, client_id, secret_key, base_url="https://api-dev.weir.ai"):
        self.client_id = client_id
        self.secret_key = secret_key
        self.base_url = base_url
        self.access_token = None
    
    def authenticate(self):
        response = requests.post(
            f"{self.base_url}/auth/token",
            auth=HTTPBasicAuth(self.client_id, self.secret_key)
        )
        response.raise_for_status()
        data = response.json()
        self.access_token = data['data']['accessToken']
        return self.access_token
    
    def make_request(self, endpoint, method='GET', data=None):
        if not self.access_token:
            self.authenticate()
        
        headers = {
            'Authorization': f'Bearer {self.access_token}',
            'Content-Type': 'application/json'
        }
        
        response = requests.request(
            method, 
            f"{self.base_url}{endpoint}",
            headers=headers,
            json=data
        )
        response.raise_for_status()
        return response.json()
```

### 3. React Hook Example
```jsx
import { useState, useEffect, useCallback } from 'react';

export function useWeirAPI(clientId, secretKey) {
  const [accessToken, setAccessToken] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  
  const authenticate = useCallback(async () => {
    setLoading(true);
    setError(null);
    
    try {
      const response = await fetch('https://api-dev.weir.ai/auth/token', {
        method: 'POST',
        headers: {
          'Authorization': 'Basic ' + btoa(`${clientId}:${secretKey}`)
        }
      });
      
      if (!response.ok) {
        throw new Error(`Authentication failed: ${response.status}`);
      }
      
      const data = await response.json();
      setAccessToken(data.data.accessToken);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  }, [clientId, secretKey]);
  
  const makeRequest = useCallback(async (endpoint, options = {}) => {
    if (!accessToken) {
      throw new Error('Not authenticated');
    }
    
    const response = await fetch(`https://api-dev.weir.ai${endpoint}`, {
      ...options,
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
        ...options.headers
      }
    });
    
    if (!response.ok) {
      throw new Error(`API request failed: ${response.status}`);
    }
    
    return response.json();
  }, [accessToken]);
  
  useEffect(() => {
    if (clientId && secretKey) {
      authenticate();
    }
  }, [clientId, secretKey, authenticate]);
  
  return { accessToken, loading, error, authenticate, makeRequest };
}
```

## Integration Patterns

### 1. Error Handling Pattern
```javascript
class WeirAPIError extends Error {
  constructor(message, code, details) {
    super(message);
    this.name = 'WeirAPIError';
    this.code = code;
    this.details = details;
  }
}

async function handleWeirResponse(response) {
  if (!response.ok) {
    const errorData = await response.json();
    throw new WeirAPIError(
      errorData.error.message,
      errorData.error.code,
      errorData.error.details
    );
  }
  
  return response.json();
}
```

### 2. Rate Limiting Pattern
```javascript
class RateLimiter {
  constructor(requestsPerHour) {
    this.requestsPerHour = requestsPerHour;
    this.requests = [];
  }
  
  async waitForToken() {
    const now = Date.now();
    const oneHourAgo = now - 3600000;
    
    // Remove requests older than 1 hour
    this.requests = this.requests.filter(time => time > oneHourAgo);
    
    if (this.requests.length >= this.requestsPerHour) {
      const oldestRequest = Math.min(...this.requests);
      const waitTime = oldestRequest + 3600000 - now;
      await new Promise(resolve => setTimeout(resolve, waitTime));
    }
    
    this.requests.push(now);
  }
}
```

### 3. Token Management Pattern
```javascript
class TokenManager {
  constructor(clientId, secretKey) {
    this.clientId = clientId;
    this.secretKey = secretKey;
    this.accessToken = null;
    this.tokenExpiry = null;
  }
  
  async getValidToken() {
    if (!this.accessToken || this.isTokenExpired()) {
      await this.refreshToken();
    }
    return this.accessToken;
  }
  
  async refreshToken() {
    const response = await fetch('https://api-dev.weir.ai/auth/token', {
      method: 'POST',
      headers: {
        'Authorization': 'Basic ' + btoa(`${this.clientId}:${this.secretKey}`)
      }
    });
    
    if (!response.ok) {
      throw new Error('Token refresh failed');
    }
    
    const data = await response.json();
    this.accessToken = data.data.accessToken;
    this.tokenExpiry = Date.now() + (data.data.expiresIn * 1000);
  }
  
  isTokenExpired() {
    return !this.tokenExpiry || Date.now() >= this.tokenExpiry;
  }
}
```

## Use Case Examples

### 1. User Registration Flow
```javascript
async function registerUser(userData) {
  try {
    // Step 1: Register user
    const registerResponse = await fetch('https://api-dev.weir.ai/auth/register', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(userData)
    });
    
    const registerData = await registerResponse.json();
    
    if (!registerData.success) {
      throw new Error(registerData.error.message);
    }
    
    // Step 2: Validate registration (if OTP required)
    if (registerData.data.requiresValidation) {
      const validateResponse = await fetch('https://api-dev.weir.ai/auth/validate/registration', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          otpSession: registerData.data.otpSession,
          otp: userData.otp
        })
      });
      
      const validateData = await validateResponse.json();
      
      if (!validateData.success) {
        throw new Error(validateData.error.message);
      }
    }
    
    return registerData.data;
  } catch (error) {
    console.error('User registration failed:', error);
    throw error;
  }
}
```

### 2. Team Management
```javascript
class TeamManager {
  constructor(apiClient) {
    this.apiClient = apiClient;
  }
  
  async createTeam(teamData) {
    return await this.apiClient.makeRequest('/org/create/team', {
      method: 'POST',
      body: JSON.stringify(teamData)
    });
  }
  
  async inviteTeamMember(teamId, memberData) {
    return await this.apiClient.makeRequest(`/org/invite/team/member/${teamId}`, {
      method: 'POST',
      body: JSON.stringify(memberData)
    });
  }
  
  async getTeams(page = 1, limit = 20) {
    return await this.apiClient.makeRequest(`/org/teams?page=${page}&limit=${limit}`);
  }
  
  async deleteTeam(teamId) {
    return await this.apiClient.makeRequest(`/org/team/${teamId}`, {
      method: 'DELETE'
    });
  }
}
```

### 3. NIL Detection
```javascript
class NILDetector {
  constructor(apiClient) {
    this.apiClient = apiClient;
  }
  
  async validateIdentity(identityData) {
    return await this.apiClient.makeRequest('/ad/validateIdentity', {
      method: 'POST',
      body: JSON.stringify(identityData)
    });
  }
  
  async checkValidationStatus(requestId) {
    return await this.apiClient.makeRequest(`/ad/validation/status/${requestId}`);
  }
  
  async uploadImage(imageFile) {
    const formData = new FormData();
    formData.append('image', imageFile);
    
    return await this.apiClient.makeRequest('/trial/uploadImage', {
      method: 'POST',
      body: formData,
      headers: {
        'Content-Type': 'multipart/form-data'
      }
    });
  }
}
```

## Testing Examples

### 1. Unit Tests (Jest)
```javascript
describe('WeirAPIClient', () => {
  let apiClient;
  
  beforeEach(() => {
    apiClient = new WeirAPIClient('test-client-id', 'test-secret-key');
  });
  
  test('should authenticate successfully', async () => {
    // Mock fetch
    global.fetch = jest.fn().mockResolvedValue({
      ok: true,
      json: () => Promise.resolve({
        success: true,
        data: { accessToken: 'test-token' }
      })
    });
    
    const token = await apiClient.authenticate();
    expect(token).toBe('test-token');
  });
  
  test('should handle authentication errors', async () => {
    global.fetch = jest.fn().mockResolvedValue({
      ok: false,
      status: 401
    });
    
    await expect(apiClient.authenticate()).rejects.toThrow('Authentication failed');
  });
});
```

### 2. Integration Tests
```javascript
describe('Weir API Integration', () => {
  test('should create and manage teams', async () => {
    const teamManager = new TeamManager(apiClient);
    
    // Create team
    const team = await teamManager.createTeam({ name: 'Test Team' });
    expect(team.data.id).toBeDefined();
    
    // Get teams
    const teams = await teamManager.getTeams();
    expect(teams.data.items).toContainEqual(
      expect.objectContaining({ id: team.data.id })
    );
    
    // Delete team
    await teamManager.deleteTeam(team.data.id);
  });
});
```

## Best Practices

<Callout type="info">
**Best Practices**: Follow these best practices for robust and maintainable Weir integrations.
</Callout>

### 1. Environment Configuration
```javascript
const config = {
  development: {
    baseUrl: 'https://api-dev.weir.ai',
    clientId: process.env.WEIR_DEV_CLIENT_ID,
    secretKey: process.env.WEIR_DEV_SECRET_KEY
  },
  production: {
    baseUrl: 'https://api.weir.ai',
    clientId: process.env.WEIR_PROD_CLIENT_ID,
    secretKey: process.env.WEIR_PROD_SECRET_KEY
  }
};

const environment = process.env.NODE_ENV || 'development';
const weirConfig = config[environment];
```

### 2. Logging and Monitoring
```javascript
class WeirAPILogger {
  logRequest(method, url, data) {
    console.log(`[Weir API] ${method} ${url}`, data);
  }
  
  logResponse(response, responseTime) {
    console.log(`[Weir API] Response: ${response.status} (${responseTime}ms)`);
  }
  
  logError(error, context) {
    console.error(`[Weir API] Error: ${error.message}`, context);
  }
}
```

### 3. Caching Strategy
```javascript
class WeirAPICache {
  constructor(ttl = 300000) { // 5 minutes default TTL
    this.cache = new Map();
    this.ttl = ttl;
  }
  
  get(key) {
    const item = this.cache.get(key);
    if (item && Date.now() - item.timestamp < this.ttl) {
      return item.data;
    }
    this.cache.delete(key);
    return null;
  }
  
  set(key, data) {
    this.cache.set(key, {
      data,
      timestamp: Date.now()
    });
  }
}
```

## Support

Need help with code examples?

- **Documentation**: Check our comprehensive API reference
- **Support Team**: Contact us at support@weir.ai
- **Status Page**: Check system status at status.weir.ai
- **Community**: Join our developer community for peer support
- **GitHub**: Check our GitHub repositories for more examples
